{% extends 'UserBundle:Default:home.html.twig' %}
{% set _locale = app.request.locale %}

{% block content %}
    <h3>
        {% trans %}Subscription creation{% endtrans %}
    </h3>

    <form action="{{ path('subscription_create', {'_locale': _locale}) }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">
                {% trans %}Create{% endtrans %}
            </button>
        </p>
    </form>

    <ul class="record_actions">
        <li>
            <a href="{{ path('subscription', {'_locale': _locale}) }}">
                {% trans %}Back to the list{% endtrans %}
            </a>
        </li>
    </ul>
{% endblock %}
